@model IEnumerable<Asn_23.Models.DuplicateFile>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.YesOrNull)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.YesOrNull)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DuplicateFileId }) |
            @Html.ActionLink("Details", "Details", new { id=item.DuplicateFileId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DuplicateFileId })
        </td>
    </tr>
}

</table>
