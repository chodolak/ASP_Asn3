@model IEnumerable<Asn_23.Models.HospitalAttended>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.HospitalName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.HospitalName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.HospitalAttendedId }) |
            @Html.ActionLink("Details", "Details", new { id=item.HospitalAttendedId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.HospitalAttendedId })
        </td>
    </tr>
}

</table>
